{
  "name": "smpmps",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --force && npm run build",
    "start": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm install --force && npm run build",
    "build:backend": "cd backend && npm install"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "axios": "^1.14.0",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "nodemailer": "^8.0.4"
  }
}
